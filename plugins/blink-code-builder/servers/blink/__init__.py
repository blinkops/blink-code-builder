"""The blink MCP server package.

Run as a module (`python3 -m servers.blink.server`) with the plugin root on
PYTHONPATH — see ../../.mcp.json. That is what makes the relative imports between
these modules resolve, and what puts `blink_shared` within reach.
"""
