# Bounded to the current major/minor line: a new release can't silently land in a
# session-start install. Widen deliberately after testing against the new version.
httpx>=0.27,<1.0
pyyaml>=6.0,<7.0
mcp>=1.2,<2.0
